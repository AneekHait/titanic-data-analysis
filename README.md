# Titanic EDA

Exploratory Data Analysis of the Titanic dataset using Python.

## Setup

```bash
make install       # or: pip install -r requirements.txt
make download      # fetch the dataset
make eda           # run the full analysis
make test          # run tests
```

## Output

- Charts are saved to `outputs/figures/`
- Analysis summary is printed to the console
